# HelpMeTest CLI Environment Variables
# Copy this file to .env and fill in your actual tokens

# Required: Your HelpMeTest API token
HELPMETEST_API_TOKEN=HELP-your-valid-token-here

# Optional: API URL (defaults to https://helpmetest.com)
HELPMETEST_API_URL=https://helpmetest.com

# Optional: Environment identifier (dev/staging/prod)
ENV=dev

# Optional: OpenAI API key for MCP testing
OPENAI_API_KEY=sk-your-openai-key-here

# Note: Health check exit codes are determined ONLY by command execution,
# not by API connectivity. Missing/invalid tokens will log warnings but
# won't prevent health checks from running and returning correct exit codes.